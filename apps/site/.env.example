# Private HTTPS endpoint that receives purpose-limited founding-expedition signups.
SIGNUP_WEBHOOK_URL=https://example.org/private-signup-webhook

# Optional bearer token sent only to the private signup endpoint.
SIGNUP_WEBHOOK_TOKEN=replace-me
