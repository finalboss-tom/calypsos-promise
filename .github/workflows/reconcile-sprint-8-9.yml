name: Reconcile Sprint 8.9 status

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/scripts/reconcile_sprint_8_9.py
      - .github/workflows/reconcile-sprint-8-9.yml

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/sprint-8-public-website-foundation
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
        with:
          version: 10.13.1
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: python .github/scripts/reconcile_sprint_8_9.py
      - name: Remove temporary reconciliation files
        run: |
          rm .github/scripts/reconcile_sprint_8_9.py
          rm .github/workflows/reconcile-sprint-8-9.yml
      - run: pnpm format
      - name: Commit reconciliation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: reconcile Sprint 8.9 status"
          git push origin HEAD:agent/sprint-8-public-website-foundation
